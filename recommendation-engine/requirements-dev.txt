# ─────────────────────────────────────────────
#  NextScene — Requirements (desenvolvimento)
#  Tudo que requirements.txt tem, mais o que só serve para
#  desenvolver: testes, notebooks e lint. Não entra na imagem
#  de produção — o Dockerfile instala só requirements.txt.
# ─────────────────────────────────────────────

-r requirements.txt

# === Testing ===
pytest>=7.4.0
pytest-asyncio>=0.21.0

# === Notebooks / Visualization ===
jupyter>=1.0.0
ipykernel>=6.26.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# === Dev / Qualidade ===
black>=23.10.0
isort>=5.12.0
flake8>=6.1.0

# === Re-treino com os ratings do app ===
# Só o script de exportação (scripts/export_app_ratings.py) fala com o
# Postgres do backend — a API do motor nunca toca em banco. Fica aqui, não em
# requirements.txt, para não entrar na imagem de produção.
psycopg[binary]>=3.1.0
