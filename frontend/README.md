
  # nextScene

  This is a code bundle for nextScene. The original project is available at https://www.figma.com/design/8d8MIKjWz4HAAnUckJVdre/nextScene.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  